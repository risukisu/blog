---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import TerminalPrompt from '../components/TerminalPrompt.astro';
import AsciiBox from '../components/AsciiBox.astro';
import AsciiDivider from '../components/AsciiDivider.astro';
import LinkPreviewTooltip from '../components/LinkPreviewTooltip.astro';
import { SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={`Projects — ${SITE_TITLE}`} description="open-source tools and experiments by risu" />
	</head>
	<body>
		<Header />
		<main>
			<TerminalPrompt command="ls -la projects/" />

			<AsciiBox title="ga4-analyzer-lite">
				<div class="project-card">
					<p class="project-desc">
						Single-file GA4 traffic analyzer — drop CSV exports, get an interactive dashboard. No install, no server, no dependencies.
					</p>
					<details class="project-features">
						<summary class="features-toggle">[features]</summary>
						<div class="features-list">
							<span class="feature-label">&gt; </span>YoY traffic comparison with 8-week moving average<br/>
							<span class="feature-label">&gt; </span>Google algorithm impact analysis (Core Updates + AI Overviews)<br/>
							<span class="feature-label">&gt; </span>Auto-generated key insights, winners &amp; losers<br/>
							<span class="feature-label">&gt; </span>Top 25 pages table with trend arrows and engagement data<br/>
							<span class="feature-label">&gt; </span>Works offline, works with file:// — just open and drop CSVs
						</div>
					</details>
					<div class="project-tags">
						<span class="tag">[html]</span>
						<span class="tag">[chart.js]</span>
						<span class="tag">[ga4]</span>
						<span class="tag">[analytics]</span>
						<span class="tag">[zero-deps]</span>
					</div>
					<div class="project-links">
						<a href="https://github.com/risukisu/ga4-analyzer-lite" target="_blank" class="project-link">
							<span class="link-glyph">[github]</span>
							<span class="link-text">risukisu/ga4-analyzer-lite</span>
						</a>
					</div>
				</div>
			</AsciiBox>

			<div class="cursor-line">
				<span class="blinking-cursor">█</span>
			</div>
		</main>
		<Footer />
		<LinkPreviewTooltip />

	</body>
</html>

<style>
	.project-card {
		display: flex;
		flex-direction: column;
		gap: 0.75em;
	}
	.project-desc {
		color: var(--text-bright);
		margin: 0;
		line-height: 1.5;
	}
	.project-features {
		color: var(--text-muted);
		font-size: 0.85em;
	}
	.features-toggle {
		cursor: pointer;
		color: var(--cyan);
		font-weight: 700;
		list-style: none;
		transition: text-shadow 0.2s;
	}
	.features-toggle::-webkit-details-marker {
		display: none;
	}
	.features-toggle::before {
		content: '▸ ';
		color: var(--cyan);
	}
	.project-features[open] .features-toggle::before {
		content: '▾ ';
	}
	.features-toggle:hover {
		text-shadow: var(--glow-cyan);
	}
	.features-list {
		margin-top: 0.5em;
		line-height: 1.8;
	}
	.feature-label {
		color: var(--green);
		font-weight: 700;
	}
	.project-tags {
		display: flex;
		gap: 0.5em;
		flex-wrap: wrap;
	}
	.tag {
		font-size: 0.8em;
		color: var(--text-muted);
	}
	.project-links {
		display: flex;
		flex-direction: column;
		gap: 0.4em;
		margin-top: 0.25em;
	}
	.project-link {
		display: flex;
		gap: 0.75em;
		align-items: baseline;
		text-decoration: none;
		transition: all 0.2s;
	}
	.project-link:hover {
		text-shadow: var(--glow-cyan);
	}
	.project-link:hover .link-text {
		color: var(--text-bright);
	}
	.link-glyph {
		color: var(--cyan);
		font-weight: 700;
		font-size: 0.85em;
		white-space: nowrap;
	}
	.link-text {
		color: var(--text-muted);
		font-size: 0.85em;
		transition: color 0.2s;
	}
	.cursor-line {
		margin-top: 2em;
	}
	.blinking-cursor {
		color: var(--green);
		animation: blink 1s step-end infinite;
	}
	@keyframes blink {
		0%, 100% { opacity: 1; }
		50% { opacity: 0; }
	}
</style>
